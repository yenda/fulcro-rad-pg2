(ns com.fulcrologic.rad.database-adapters.pg2
  "This namespace exists for the fully qualified keywords and symbols
  used in the RAD pg2 PostgreSQL plugin. For the usage and implementation
  of the plugin, see `com.fulcrologic.rad.database-adapters.pg2.plugin`")
